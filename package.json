{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build && pulp test -- -w hello -w world"
  },
  "dependencies": {
    "yargs": "^11.0.0"
  },
  "devDependencies": {
    "pulp": "^12.0.0",
    "purescript-psa": "^0.5.0",
    "purescript": "^0.12.0",
    "rimraf": "^2.5.4"
  }
}
